name: "Test"
on:
  push:
  pull_request:
    branches:
      - master
    types: [labeled, edited]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: ECHO TIME
        run: echo 'trigger time'

      - name: HELLO
        run: echo 'hello'

    