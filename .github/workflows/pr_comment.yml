name: "PR Comment"
on:
  pull_request_review_comment:
    types:
      - "created"
  pull_request_review:
    types:
      - "submitted"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: label
        run: echo "comment"

    